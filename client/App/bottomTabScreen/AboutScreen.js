import React from 'react';
import { Text, View } from 'react-native';

function AboutScreen({route}) {
    // const { title } = route.params;
    return (
       <View>
         <Text>About Screen "f"</Text>
       </View>
    );
}

export default AboutScreen;